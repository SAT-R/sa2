Tilesets' layouts are stored as 4bpp data, but metatiles contain an index per tile, signifying which palette this tile is using.
So the Tileset data (<stage>.png) is only stored with one palette, while <stage>.pal contains 16 palettes.

